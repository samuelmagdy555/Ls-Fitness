import 'package:flutter/material.dart';

class ExerciseUI extends StatelessWidget {
  const ExerciseUI({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(

    );
  }
}
